# QA Round 119 Report — 2026-05-27 23:09

**Agent:** QA-Mariana
**Persona this round:** Skeptical Evaluator (hour 23 mod 8 = 7)
**Session state:** Disconnected RDP session — blocker from rounds 117/118 still in effect

## Phase 0 — CI Guardian
- Last CI SHA: `cf3e1fd` → Current HEAD: `754ed6d`
- 93 files / +8,322 / −2,855 since last CI (excluding QA reports/orc reviews)
- Desktop + integration + e2e suites **could not run** — no interactive desktop in this session
- Targeted unit tests for the 5 status:done issues (#786 #788 #807 #840 #843): **109 passed / 24 skipped** (`test_input_keyboard`, `test_input_strategies`, `test_capture_popup_843`, `test_stale_pid_routing*`, `test_type_newline_840`, `test_winui_menu_click_786`) — code health for those fixes confirmed at the unit level
- Full unit suite run hung on a browser-CDP test (unrelated to current work — `test_browser*` not properly marked); did not block other phases
- Did NOT update `.last-ci-sha` since the desktop suite did not actually run

**Reported:** CI Desktop Tests: skipped (no interactive desktop in QA session; blocker #863)

## Phase 1 — Verify status:done issues
5 remain open, all SendInput-dependent:
- #786 — UWP Notepad menu click regression
- #788 — type --app stale PID (silent failure)
- #807 — press --app focus mismatch
- #840 — type drops literal newlines (default mode)
- #843 — capture --app excludes popup menu

**Outcome:** all 5 BLOCKED — same root cause as round 118 (#863). The QA agent session 1 is still `Disconnected` per `query session`; session 2 (`console`) holds the active desktop binding. Every `naturo` desktop call returns `NO_DESKTOP_SESSION`. Code + unit tests are green for all 5.

Updated #863 with the round 119 data point.

## Phase 2 — E2E Desktop Testing
**Skipped** — no interactive desktop. Nothing changed from round 118.

## Phase 3 — Exploratory Testing
Probed exit-code and JSON-envelope contracts across read-only vs input commands. Findings:
- Read-only (`see`, `capture`, `list apps`, `app launch`): exit 1 + clean `Error: ...` on NO_DESKTOP_SESSION ✓
- Input (`type`, `press`, `click`): exit **2** + Click `Usage:` banner on NO_DESKTOP_SESSION ✗
- With `-j` flag, all commands return exit 1 + proper JSON envelope ✓

Root cause: `naturo/cli/interaction/_common.py:_get_backend` raises `click.UsageError(str(exc))` (exits 2) instead of `click.ClickException` (exits 1) when `json_output=False`. Same pattern at line 781 for `BACKEND_ERROR`. The sister `core/_common.py:_get_backend` has the same code but the calling commands (`see_cmd` etc.) wrap it in a top-level `try/except` that emits a clean error — input commands have no such wrapper.

→ Filed **#866** (P2, v0.3.4).

## Phase 4 — Skeptical Evaluator Simulation
Adopted the persona of someone comparing naturo vs pywinauto for adoption. Key friction observed:

1. **Exit-code inconsistency** (filed #866) — a serious turn-off for the "compare vs pywinauto" persona. pywinauto raises Python exceptions consistently; naturo's exit-code contract differs by command family.
2. **`Usage:` banner on a non-usage error** — actively misleading. A skeptic seeing `Usage: naturo type [OPTIONS] [TEXT]` followed by `Error: No interactive desktop in this session` would assume they passed wrong arguments, not that the environment is wrong.
3. **JSON path is the "good" path** — only `-j` consistently returns the right exit code + envelope across all commands. Suggests the non-JSON path is an afterthought.

These would all be flagged in the first 15 minutes of a serious evaluator's review. Issue #866 captures the most concrete instance.

## Phase 5 — Regression Test Cases
Ran what could run without a desktop:

| TC | Result | Consec | Notes |
|----|--------|--------|-------|
| TC-0041 (`json-mode-exit-code-zero-on-failure`) | PASS | 29 | All 3 probes exit 1, both -j and non-JSON |
| TC-0046 (`see-error-exit-code-zero`) | PASS | 20 | Exit 1 in both modes |
| TC-0054 (`input-cmd-exit-code-on-no-desktop`) | FAIL | 0 | New — #866 just filed |

All other active TCs require an interactive desktop and were skipped this round.

## Phase 6 — Evolve Test Cases
- **Created TC-0054** — `exploratory/input-cmd-exit-code-on-no-desktop.yaml`, P2, source_issue #866
- Updated CATALOG.md
- No cases retired this round (issues unchanged)
- No cases deleted

## Phase 7 — Issues Filed
- **#866** [bug, P2, from:qa, v0.3.4] — input commands exit 2 with `Usage:` on NO_DESKTOP_SESSION instead of exit 1
- Commented on **#863** with round 119 data point (same blocker persists)

## Phase 8 — Status

Total active test cases after this round: 33 active, 22 retired (counted from CATALOG.md).

### Top 3 Risks
1. **QA verification stuck at 5 issues for v0.3.2** — desktop session blocker (#863) has now persisted 3 rounds. v0.3.2 cannot ship until either (a) the QA agent runs from console session 2, or (b) these 5 fixes are accepted on unit-test evidence + Ace manual smoke. No new path forward this round.
2. **Self-hosted runner offline day 59** (#842) — still no Ace response since 2026-05-08. Cloud VM proposal (#860) still unassigned day 20.
3. **CLI error-handling consistency** (new this round, #866) — small per-command divergence accumulates into a fragile CLI contract. Not P0, but the kind of thing a serious evaluator flags.

---
**[QA-Mariana]**
