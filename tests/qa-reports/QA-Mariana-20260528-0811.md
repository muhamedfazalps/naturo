# QA Report — QA-Mariana — Round 128

**Date:** 2026-05-28 08:11
**Persona:** First-time User (hour 08 mod 8 = 0)
**Runner:** NATUROBOT
**naturo version:** 0.3.1
**Session:** NO_DESKTOP_SESSION (worse than RDP+UIPI — desktop not bindable from this shell)

## TL;DR

Session has no interactive desktop, so all desktop verification stayed blocked on #863. Phase 1 unit tests for the 5 status:done issues still pass (93/93). Phase 4 first-time-user persona surfaced a real UX dead-end: typing intuitive verbs (`naturo launch`/`open`/`run`/`start`/`screenshot`) gets either no suggestion or the wrong (hidden) one. Filed **#880**. Scope-expanded **#866** (highlight/move/drag), **#867** (hidden `window`), **#876** (`visual list -j`).

## Phase 0 — CI Guardian
**Result:** Skipped — no code changes since last CI run (`e7615ce`). Diff between `e7615ce..4bc1d2f` is only QA reports and test-case YAMLs.

## Phase 1 — Verify Dev fixes
**Status:done queue (5 issues, all SendInput-dependent, all blocked by #863):**

| Issue | Title | This-round result |
|-------|-------|-------------------|
| #786 | UWP Notepad menu click | Unit tests **pass** (9/9). Desktop blocked. State unchanged from R117 partial-verify. |
| #788 | type --app stale PID | Production code correct per R117 comment. Test scaffolding bug → #870. Desktop blocked. |
| #807 | press --app focus mismatch | Unit tests **pass** (66/66). Desktop blocked. State unchanged. |
| #840 | type drops newlines | Unit tests **pass** (3+8/3+8). Desktop blocked. State unchanged. |
| #843 | capture excludes popups | Unit tests **pass** (7/7). Desktop blocked. State unchanged. |

Combined: **93/93 unit tests pass**, runtime verification still blocked by #863. No new partial-verify comments needed — the prior R117 comments still describe the current state.

## Phase 2 — Professional Desktop E2E Testing
**Skipped — NO_DESKTOP_SESSION.** Every `naturo see`/`capture`/`click`/`app launch`/`list apps` call returns `NO_DESKTOP_SESSION` error. Session worse than the RDP-blocked one #863 describes (this one cannot even read the tree).

## Phase 3 — Professional Exploratory Testing (contract-level only)
Focused on CLI surface that doesn't need a live desktop. Findings (all confirmed against existing or new issues):

| Surface | Behavior | Status |
|---|---|---|
| `naturo see/capture/list apps/find/get/set -j` | exit 1, JSON envelope ✓ | OK |
| `naturo see/capture/...` (no -j) | exit 1, plain "Error:" line ✓ | OK |
| `naturo click/type/press` (no -j) | exit **2** + Click usage page | #866 (still) |
| `naturo click/type/press -j` | exit 1, JSON envelope ✓ | OK (since #866 fix?) |
| `naturo move/drag` (no -j) | exit 2 + Click usage page | #866 extension |
| `naturo move/drag -j` | exit **2** + Click usage page (NOT JSON) | #866 + #872 (both) |
| `naturo highlight` (no -j) | exit **2** + Click usage page | #866 extension — NEW evidence |
| `naturo highlight -j` | exit 1, JSON envelope ✓ | OK |
| `naturo selector list -j` (empty) | `{}` — no `success`, no `selectors` array | #876 |
| `naturo record list -j` | `{"recordings":[...]}` — no `success` | #876 |
| `naturo visual list -j` | `{"baselines":[]}` — no `success` | #876 extension — NEW evidence |
| `naturo wait abc -j` | exit 2 + Click usage page (no JSON) | #872 |
| `naturo wait 0.1 -j` | exit 0, `{"success":true,...}` ✓ | OK |
| `naturo wait -1` | exit 2, treated as flag | by-design |
| `naturo --version`/`--help` with -j | plain text, no envelope | #874 |
| `naturo app windows -j` | returns real window list w/o desktop guard | #878 (still) |

## Phase 4 — Simulated User: First-time User
**Approach:** opened a fresh terminal, ignored docs, typed intuitive English verbs.

Key intuition failures:

1. `naturo launch notepad` → `Error: No such command 'launch'.` — no suggestion.
2. `naturo open notepad` → same.
3. `naturo run notepad` → same.
4. `naturo start notepad` → same.
5. `naturo screenshot` → `Did you mean 'snapshot'?` — wrong (hidden debug command) — should suggest `capture`.
6. `naturo windows` → `Did you mean 'window'?` — `window` is hidden + deprecated.
7. `naturo apps` → `Did you mean 'app'?` ✓ (works because top-level near-match).
8. `naturo help` (no dashes) → works ✓ (Click is forgiving on the help verb).

Filed **#880** for the subgroup-suggestion gap. Hidden-command leak is #867 — added scope expansion (window also leaks).

## Phase 5 — Regression Test Cases
Ran the subset that does not need a live desktop:

| TC | Issue | Result | Notes |
|---|---|---|---|
| TC-0054 | #866 | **fail** (bug present) | click/type/press still exit 2 without -j |
| TC-0055 | #867 | **fail** (bug present) | snapshot still hidden + still suggested for typos |
| TC-0059 | #872 | **fail** (bug present) | wait abc -j → Click usage, no JSON |
| TC-0061 | #874 | **fail** (bug present) | -j --version still plain text |

YAMLs already date-stamped 2026-05-28 by earlier rounds today; no churn needed.

Desktop-required cases (TC-0003, 0024, 0026, 0027, 0029, 0042, 0043, 0045 etc.) — **not run** (no desktop session this round).

## Phase 6 — Test Case Evolution
- **Created TC-0067** — `exploratory/typo-suggester-ignores-subgroups.yaml` (covers #880).
- **Updated CATALOG.md** with TC-0067 entry.

## Phase 7 — Issues
- **Filed [#880](https://github.com/AcePeak/naturo/issues/880)** — `bug: CLI typo suggester ignores subgroup commands — first-time users hit dead end on 'launch'/'open'/'screenshot'` (P2, v0.3.4).
- **Commented on #866** — `highlight` exhibits exit-2 + Click-usage-page on runtime errors; `move`/`drag` don't even honor `-j`.
- **Commented on #867** — hidden `window` group also leaks via typo suggestions.
- **Commented on #876** — `visual list -j` is a third instance of the missing-`success`-field pattern.

## Phase 8 — Status
This file.

## Top 3 Risks
1. **#863 (P0 ship-gate)** still blocks v0.3.2 — 5 status:done issues can't be desktop-verified. This round's session is even more constrained (no desktop at all). 21 days since the workaround was identified, no progress on launching QA from console session.
2. **CLI contract drift** — at least 6 distinct exit-code / JSON-envelope inconsistencies (#864, #865, #866, #872, #874, #876) are still open after 3+ persona rounds. Each is small but together they signal an absence of a unified "-j contract" test layer. Worth adding one CI test that walks every subcommand and asserts (a) `-j` always produces parseable JSON, (b) `success: bool` field always present, (c) `-j` exit codes are 0/1 only.
3. **First-time UX friction (#880, #867)** — a brand-new user typing `naturo launch chrome` gets a dead end. This is the very first moment of contact and it currently fails. Recommend either flat-verb aliases (`launch`/`open`/`screenshot`) at top level, or a smarter suggester that walks subgroups.

---
**[QA-Mariana]**
