# QA Report — Round 149
- **Date:** 2026-05-29 05:07
- **Agent:** QA-Mariana (running on NATUROBOT)
- **Persona:** Accessibility User (persona index 5, hour 05) — keyboard-only navigation, screen-reader semantics
- **HEAD:** `c4342ff` (develop)
- **Environment:** Windows 11 Pro 10.0.26200, naturo 0.3.1, **NO interactive desktop session** (SSH/service session; #863 unresolved)

## Phase 0 — CI Guardian
**Skipped.** Only commits since last CI sha `17aefe6` are QA reports / testcase YAMLs / logs — **no source changes** (verified via `git diff --stat 17aefe6..c4342ff`: 30 files, all under `agents/qa/`, `tests/qa-reports/`). No desktop session to run the desktop/E2E suite regardless. `.last-ci-sha` left at `17aefe6` (CI not actually executed).

## Phase 1 — Verify Dev Fixes (status:done)
5 `status:done` issues remain — **all blocked, none runtime-verifiable in this session**:
- #786 (click eN UWP menu), #788 (type --app stale PID), #807 (press --app wrong process), #840 (type drops newlines), #843 (capture --app excludes popup menu).
- All require an interactive desktop + SendInput, blocked by #863 (UIPI: SendInput returns ERROR_ACCESS_DENIED from this session). #840 already carries a documented **partial verification** (code + 11 unit tests pass; physical desktop test blocked). Left as `status:done` — **not rejected**, since the fixes may be correct; the blocker is infra, not the patch. Ship gate still depends on a console-session QA run that is not occurring here.

## Phase 2/3/4 — Professional + Accessibility-persona testing
Desktop-dependent E2E (launch→see→click→type→capture) not possible (NO_DESKTOP). Focused on the keyboard-only / screen-reader surface that IS testable (CLI contract, key parsing, JSON envelopes):

**Keyboard-navigation surface (`press`)** — the accessibility user's #1 command:
- `naturo press tab` (and `enter`, `ctrl+c`, `alt`, `ctrl`, `shift`, `win`, `f6`, `f10`, `apps`, arrows, Home/End/PageUp) → **all exit 2 + Click `Usage:` banner**, because the runtime NO_DESKTOP_SESSION error is raised as `UsageError`. This is **#866 / TC-0054**, confirmed persisting. For a keyboard user this misframes an environment failure as a bad-key-spec error.
- `naturo press tab -j` → correctly exits **1** with `{success:false, code:NO_DESKTOP_SESSION}`. JSON path is right; text path is wrong (matches #866 analysis).
- The guard fires **before** key-spec parsing, so navigation-key *acceptance* (Tab/Apps/F6/etc.) is unobservable here → captured as **TC-0089 (blocked)**.

**Accessibility schema / filters (`find`, `see`)** — confirmed unchanged on HEAD:
- `find --focusable` / `--enabled` / `--visible` → all rejected (`No such option`); `--focusable`/`--enabled` suggest `--actionable`, `--visible` suggests nothing. Confirms **#896** gap (focusable/enabled/on-screen collapsed into one `--actionable` boolean).
- `keyboardShortcut` always null (#886) and missing accessibility props (IsKeyboardFocusable/HasKeyboardFocus/HelpText/IsEnabled/IsOffscreen/LocalizedControlType) per #896 — no source change since filed.
- Posted a consolidated R149 confirmation comment on #896 (was 0 comments) — it is the single highest-leverage item for naturo's stated "screen-reader / keyboard-only" audience.

## Phase 5 — Regression (no-desktop-safe subset, HEAD `c4342ff`)
Source is logically identical to e346036 (R145–148 ran the full suite today); ran the guard subset to re-confirm state, did not inflate counters already bumped today.

| TC | Issue | Result | Evidence |
|----|-------|--------|----------|
| TC-0065 | #878 `app windows -j` | **FAIL (bug present)** | exit 0, `success:true`, returns real windows — **even leaks the QA terminal's own window** (Microsoft.WindowsTerminal) |
| TC-0062 | #875 `dialog detect -j` | **FAIL (bug present)** | exit 0, `{success:true, dialogs:[], count:0}` |
| TC-0079 | #893 `wait --gone -j` | **FAIL (bug present)** | exit 0, `{success:true, found:true, wait_time:0.0, warnings:[]}` |
| TC-0046 | see error exit code | **PASS** | `see --app` (text) exits 1 |
| TC-0088 | #885 guard positive-lock | **PASS** | see/capture/list windows/list apps/list screens all exit 1, `NO_DESKTOP_SESSION`; consecutive_passes 1→2 |

Silent-failure cluster (#885 epic: #875/#878/#883/#893) **unchanged** — `success:true` + wrong/leaked data, the worst class for AI agents. Correct guards still hold.

## Phase 6 — Test case evolution
- **Created TC-0089** (`exploratory/keyboard-navigation-key-matrix.yaml`, status: **blocked**) — locks in that `press` must accept AND physically execute the full keyboard-navigation matrix (Tab/Shift+Tab/Apps/F6/F10/arrows/Home/End/PageUp), broadening TC-0035 (standalone modifiers only). Blocked until a desktop session exists (guard masks key parsing per #866/#863).
- Updated TC-0088 (pass, →2), TC-0065 (fail, terminal-window-leak note) with R149 data.
- No retirements (no source-issue closed + 5 passes this round).

## Phase 7 — Issues filed
**None.** Every R149 finding maps cleanly to an open issue (#866, #875, #878, #886, #893, #896, #863). No genuine new defect surfaced — manufacturing one would be dishonest. Added one value-adding comment to #896.

## Top 3 Risks
1. **#885 silent-failure cluster (P0) unfixed** on `c4342ff` — `success:true` with wrong/leaked data (#878 even leaks the QA terminal window). Dev-Sirius idle 53 days; this is the v0.3.2 ship gate.
2. **Accessibility audience underserved** — keyboard-only/screen-reader users (a stated target segment) hit a triple gap: no focus introspection / focusable / enabled / on-screen filters (#896), `keyboardShortcut` always null (#886), and `press` runtime errors misframed as usage errors (#866). All P1/P2, all unfixed.
3. **CI desktop coverage dark day 59+** (#842 runner offline, #860 cloud-VM unassigned) + the 5 `status:done` fixes un-verifiable without a console session (#863) — guard regressions on currently-correct commands would go uncaught except via TC-0088's positive lock, which itself can only run from a no-desktop session.

**[QA-Mariana]**
