# QA Report — Round 95
**Agent**: QA-Mariana
**Date**: 2026-04-02 13:20
**Commit**: b032097 (develop)
**Persona**: Accessibility User (index 5)

## CI Desktop Tests
- Desktop/Integration: 1 failed, 1 passed, 8 skipped (commit b032097)
  - FAIL: test_detect_calculator_has_uia — Calculator only reports vision, not UIA (known #785)
- E2E: 9 passed, 3 xfailed, 1 xpassed

## Phase 1 — Dev Fix Verification
- No `status:done` issues pending verification

## Phase 2 — E2E Testing
### Notepad
- Launch: PASS
- See (element tree): PASS — 43 elements detected, roles/names correct
- Click text area (e3): PASS — screenshot verified
- Type text: PASS — "QA Round E2E Test - Phase 2 Verification" confirmed in screenshot (40 chars)
- JSON output valid: PASS
- Click menu (e14): FAIL — reports success but File menu does NOT open (known #786)
- App quit: PASS

### Calculator
- Launch: reported success (PID 104876) but Calculator NOT visible and NOT in `naturo list apps` — FAIL (known #785)

## Phase 3 — Exploratory Testing
- Error paths (click --app nonexistent, type --id e99999, see --depth -1): All return non-zero exit codes — PASS
- JSON error output: Structured with code/message/suggested_action — PASS
- JSON + error: Duplicate human-readable text to stderr alongside JSON — FAIL (known #783)
- Out-of-bounds click (99999, 99999): No error, exit 0 — FAIL (known #787)
- Empty string type: Properly rejected — PASS
- Invalid flag (--verbose): Exit code 2 — PASS
- Special chars/unicode/emoji: All typed correctly — PASS
- Character count discrepancy: naturo reports 25 chars, Windows shows 27 (emoji surrogate pairs counted differently) — minor

## Phase 4 — Accessibility User Simulation
### What works for keyboard-only users:
- Element roles (MenuItem, Button, Document, Tab) properly exposed for screen readers
- Element names include keyboard shortcut hints (e.g., "加粗(Ctrl+B)")
- `naturo click eN` works reliably for document area and toolbar buttons
- `naturo type --app` works correctly
- `naturo press "ctrl+a"` and `naturo press "delete"` work for text editing

### What is broken for keyboard-only users:
- **`press --app` hotkey routing (NEW #807)**: `naturo press "alt+f" --app notepad` sends keystroke to wrong process. Routing shows `focused_pid: 96668` != target `pid: 37476`. File menu does not open.
- **Menu click failure (#786)**: Neither `naturo click eN` nor `--on "文件"` opens UWP Notepad menus
- **Critical gap**: Accessibility users have NO working path to access menus — both keyboard shortcuts and click-by-ID fail

## Phase 5 — Regression Test Cases
- Background agents dispatched for 28 active test cases
- Manual verification confirmed: TC-0043 (Calculator invisible) FAIL, TC-0031 (menu click) FAIL, TC-0042 (JSON stderr) FAIL, TC-0044 (OOB coords) FAIL

## Phase 6 — Test Case Evolution
- Created: TC-0047 — press --app hotkey sends to wrong process when target is not focused (#807)
- Total active test cases: 28

## Phase 7 — Issues Filed
- **#807** (P1): press --app sends hotkey to wrong process when target is not focused

## Summary
| Metric | Value |
|--------|-------|
| Issues verified | 0 (none pending) |
| E2E apps tested | 2 (Notepad, Calculator) |
| New issues filed | 1 (#807) |
| New test cases | 1 (TC-0047) |
| CI desktop tests | 1 fail, 1 pass, 8 skip |
| CI E2E tests | 9 pass, 3 xfail, 1 xpass |

## Top 3 Risks
1. **Menu interaction completely broken on UWP Notepad** — #786 blocks all menu access (click eN, --on, keyboard shortcuts). Accessibility users and automation scripts cannot access File/Edit/View menus.
2. **Calculator UWP invisible** — #785 means Calculator cannot be automated at all. This is a core UWP detection issue.
3. **Press --app doesn't focus target** — #807 means keyboard shortcuts are unreliable when target is a background window. Combined with #786, this leaves accessibility users with no way to navigate menus.
