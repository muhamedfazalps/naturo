# QA-Mariana Round 132 Report
**Date**: 2026-05-28 12:07
**Persona**: Power User (hour 12 mod 8 = 4)
**Runner**: NATUROBOT
**Session state**: NO_DESKTOP_SESSION (agent shell cannot bind to interactive desktop)
**Milestone**: v0.3.2 (21 open, ship-gated by #863)

---

## Phase 0 — CI Guardian
**Skipped.** `.last-ci-sha = 2d15274` vs `HEAD = 38cea0c`. The only commit in between is `38cea0c qa: round 131 report — Chinese User persona, filed #884 [skip ci]`. No source or test changes — running the full desktop+integration+e2e suite would not exercise any new code. SHA advance deferred until the next non-QA commit lands.

## Phase 1 — Verify Dev Fixes
**Skipped (blocked).** Five `status:done` issues remain (#786, #788, #807, #840, #843) — all SendInput-dependent, all blocked by #863. Orc-Mycelium escalated #863 earlier today (2026-05-28) and flagged that additional QA data points carry diminishing returns until ownership is assigned. No new comment from QA this round.

Notable: today's session is *broader* than #863's framing. #863 documents "INPUT injection blocked, read-only operations work fine." Today every read-only desktop operation also returned `NO_DESKTOP_SESSION` (see / capture / app launch / list apps / find / drag / scroll / menu-inspect / get / set / move / wait). The agent shell isn't on an RDP session today — it's not bound to the interactive desktop at all. Worth folding into #863 if/when an owner picks the issue up.

## Phase 2 — E2E Desktop Testing
**Skipped (no desktop).**

## Phase 3 — Professional Exploratory Testing — Contract Surface

With desktop ops unavailable, focused the round on the JSON / exit-code / CLI-contract surface, which doesn't require desktop binding. 33 CLI probes across:

### Confirmed open bugs (still failing)
| Issue | Test case | Probe | Observation |
|-------|-----------|-------|-------------|
| #864  | TC-0052   | `naturo highlight --id e1 -j` | "No such option: --id" — only `click` accepts `--id eN` |
| #869  | TC-0057   | `naturo desktop list -j` | `Install it now? [Y/n]:` prompt leaks into `-j` output |
| #871  | TC-0058   | `naturo find foo --pid 1` etc. | Window-targeting flag matrix still inconsistent |
| #874  | TC-0061   | `naturo --version -j`, `naturo -j --help` | Eager options bypass `-j` envelope (plain text) |
| #875  | TC-0062   | `naturo dialog detect -j`, `taskbar list -j`, `tray list -j` | `success:true []` silent success in NO_DESKTOP_SESSION |
| #876  | TC-0063   | `naturo selector list -j` → `{}`, `naturo record list -j` → `{recordings:[…]}` | No top-level `success` field |
| #877  | TC-0064   | `naturo get e99 -j` | `UNKNOWN_ERROR` code, no `suggested_action` |
| #878  | TC-0065   | `naturo app windows -j` | Returns real window list with `success:true` despite NO_DESKTOP |
| #880  | TC-0067   | `naturo snapsho` | "Did you mean 'snapshot'?" — hidden subcommand still leaks |
| #884  | TC-0071   | `see -j` / `app launch -j` / `get e1 -j` | Error envelope still has three shapes (3 / 6 / 2 fields) |

### Passing
| Test case | Observation |
|-----------|-------------|
| TC-0042 (json-stderr-duplicate-error) | `naturo see -j` writes valid JSON to stdout, stderr empty (0 bytes). `consecutive_passes` 3 → 4. |

## Phase 4 — Power User Persona Simulation

A real Power User scenario ("10+ apps open, multi-monitor, 150% DPI, `--app` filter precision") cannot run without desktop. Pivoted to the Power-User CLI-friction surface — the part of Power-User experience that's already broken even with a desktop.

### Finding: `naturo app *` subgroup has an 11-subcommand × 5-flag matrix where every cell is different

Probed every subcommand of `naturo app` (11 of them) against the window-targeting flag family (`--app`, `--window`, `--hwnd`, `--pid`, positional NAME):

| Subcommand   | `--app` | `--window` | `--hwnd` | `--pid` | Positional NAME |
|--------------|:-------:|:----------:|:--------:|:-------:|:---------------:|
| launch       |   ✓     |     ✗      |    ✗     |    ✗    |       ✓         |
| quit         |   ✓     |     ✗      |    ✗     |    ✓    |       ✓         |
| focus        |   ✓     |     ✓      |    ✓     |    ✗    |       ✓         |
| close        |   ✓     |     ✓      |    ✓     |    ✗    |       ✓         |
| **minimize** | **✗**   |     ✓      |    ✓     |    ✗    |       ✓         |
| **maximize** | **✗**   |     ✓      |    ✓     |    ✗    |       ✓         |
| **restore**  | **✗**   |     ✓      |    ✓     |    ✗    |       ✓         |
| inspect      |   ✓     |     ✗      |    ✗     |    ✓    |       ✓         |
| find         |   ✗     |     ✗      |    ✗     |    ✓    |       ✓ (req)   |
| **move**     | **✗**   |     ✓      |    ✓     |    ✗    |       ✓         |
| relaunch     |   ✓     |     ✗      |    ✗     |    ✗    |       ✓         |

Every row is different. A Power User scripting

```bash
naturo app focus --app notepad && naturo app minimize --app notepad
```

fails on line 2 with `No such option: --app`.

`#871` documents this for `app focus` and `app quit` only — it does not enumerate `minimize`, `maximize`, `restore`, `move`. Posted the expanded matrix to #871 (issue comment 4560752948) so the harmonization fix covers the entire `app` subgroup, not just two subcommands.

### Phase 4 net new
- 0 new GitHub issues filed
- 1 issue comment posted (#871)
- 1 test case extended (TC-0058)

## Phase 5 — Regression
- 11 contract-level cases re-run: 1 pass (TC-0042 → 4), 10 fail (all map to open issues, all stay at 0 passes)
- 50 cases active; ~40 require a real desktop and were skipped

## Phase 6 — Evolve Test Cases
- **Extended**: TC-0058 (`window-targeting-flag-matrix.yaml`) — added probes for `app minimize`, `app maximize`, `app restore`, `app move` accepting `--app`
- **Created**: none
- **Retired**: none

## Phase 7 — Issues Filed
- 0 new issues
- 1 comment: #871 (Power User round 132 — expanded `app *` matrix)

## Top 3 Risks

1. **#863 ship-gate still owns v0.3.2.** 21+ days, ownership unassigned, today's session was even more constrained (whole-desktop loss vs input-only). Worth folding the broader scope into #863's body when an owner picks it up.
2. **`naturo app *` flag matrix is the worst-offender CLI subgroup we have.** 11 subcommands, no two share the same window-targeting flag set. #871 documents this incompletely — the expanded matrix posted today shows `minimize`/`maximize`/`restore`/`move` are also broken. Harmonize the entire `app` subgroup.
3. **Contract-drift cluster (10 open bugs) is the next-highest QA leverage.** A pytest contract harness running the matrix from TC-0058/0071/0062/0063 etc. as a single check would prevent the next 3-5 of this class.

**[QA-Mariana]**
