# QA Round 147 — Chinese User Persona
Last updated: 2026-05-29 03:13 UTC
Current round: 147
HEAD: e2afdcb (Round 146 report)
Session: NO_DESKTOP_SESSION (NATUROBOT, headless / non-console)
Persona: Chinese User (hour 03 mod 8 = 3)

## This Round

- **CI Desktop Tests**: skipped — develop HEAD `e2afdcb` is a QA-report commit only (`[skip ci]`); no new source code since last CI SHA `17aefe6`. Self-hosted runner `ROBOT-COMPILE` also still offline (#842, day 59).
- **Issues verified**: none — 5 `status:done` (#786, #788, #807, #840, #843) all SendInput-dependent and blocked by #863. Remain awaiting console-session QA per restructured ship gate.
- **E2E tests (Phase 2)**: skipped (NO_DESKTOP_SESSION). Used the window into the bug surface instead — verified guard behavior, exit-code drift, and JSON envelope shape across non-desktop commands.
- **Regression (Phase 5)**: 12 cases re-confirmed on HEAD `e2afdcb`:
  | TC | result | issue |
  |-----|--------|-------|
  | TC-0046 (see exit code on error) | **PASS** (29 consecutive) | n/a |
  | TC-0054 (input cmd exit code on no_desktop) | FAIL | #866 |
  | TC-0061 (eager option bypass JSON envelope) | FAIL | #874 |
  | TC-0063 (selector/record/visual list envelope) | FAIL + extended | #876 |
  | TC-0064 (get/set UNKNOWN_ERROR) | FAIL | #877 |
  | TC-0067 (typo suggester ignores subgroups) | FAIL | #880 |
  | TC-0074 (clipboard set missing --file/stdin) | FAIL | #888 |
  | TC-0079 (wait --gone silent success) | FAIL + Chinese repro | #893 |
  | TC-0080 (JSON ensure_ascii escapes Chinese) | FAIL + selector repros | #894 |
  | TC-0083 (missing arg exit code drift) | FAIL | #897 |
  | TC-0084 (-h short flag not accepted) | FAIL | #899 |
- **Persona findings (Phase 4 — Chinese User)**:
  - **Selector roundtrip with Chinese names** works end-to-end at the storage layer: save/load/show/delete with `记事本`/`保存按钮` succeeds, file lands at `~/.naturo/selectors/记事本.json` (literal UTF-8 filename), non-`-j` list prints literal Chinese. Captured as new test case TC-0087 (regression-prevention).
  - **JSON wire format is ASCII-escaped** for every Chinese field across `selector save/load/show/delete`, `app windows -j` (window titles `计算器`/`文件资源管理器`/`要恢复页面吗？`), `record show <chinese> -j`. Extended #894 with the selector domain repros (previously listed only clipboard / type --file / app windows).
  - **Selector show envelope drift** — `naturo -j selector show <nonexistent>` returns `{"user": {}, "builtin": {}}` indistinguishable from "existent app with zero selectors", and has no `success` field. Extended #876 with this fourth callsite (in addition to `selector list`, `record list`, `visual list`).
  - **wait --gone with Chinese selector** still bypasses NO_DESKTOP_SESSION (TC-0079) — `naturo wait --gone '记事本'` returns silent success exit 0 identical to ASCII case. No drift between languages on the bug path; reinforces #893's "guard isn't even reached" diagnosis.
- **New test cases created**: TC-0087 (`regression/selector-chinese-name-roundtrip.yaml`) — regression-prevention for Chinese-name selector flow.
- **Test cases cleaned up**: none.
- **New issues created**: none — both new findings (selector-show envelope, selector-domain ascii-escape) are extensions to existing open issues (#876 + #894).
- **Issue comments**: #876 (selector show as 4th drift callsite), #894 (selector domain repros + selector_cmd.py addition to fix surface).
- **Total active test cases**: 66 (was 65; +TC-0087)
- **Tests run**: ~30 CLI invocations across exit-code/envelope/Chinese-roundtrip matrices.

## Top 3 Risks

1. **Selector-show envelope inconsistency is more harmful than `selector list -j`** — both return `{}`-shaped output, but `selector show <typo>` is silently indistinguishable from `<valid-app-no-selectors>`. A Scripter who mistypes an app name gets a clean exit 0 + empty body and proceeds to act on "no saved selectors" assumption. Recommend bumping #876's scope/priority once Dev-Sirius starts on it (still P2 today, but #876 + this drift together amount to a 4-callsite fix).

2. **Chinese silent-failure expansion is structurally identical to ASCII** — `wait --gone 记事本` reaches the same code path as `wait --gone foo` and bypasses NO_DESKTOP_SESSION identically. Cluster #885 (Python-only middleware) will fix both at once; no separate Chinese-locale variant needed. Confirms the right fix shape.

3. **Five `status:done` issues remain blocked on console-session QA** (#786/#788/#807/#840/#843) — unchanged from R146. Restructured ship gate cannot close without it. Console-session QA is on Ace's plate per current STATE.md ownership; no QA action available until then.

---
**[QA-Mariana]**
