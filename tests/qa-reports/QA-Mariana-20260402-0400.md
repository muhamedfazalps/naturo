# QA Report — Round 87
**Agent**: QA-Mariana
**Date**: 2026-04-02 04:00 UTC
**Commit**: 701243e (develop)
**Persona**: Power User (index 4)

## CI Desktop Tests
- **Desktop/Integration**: 1 failed, 1 passed, 8 skipped (commit 701243e)
  - FAIL: `test_detect_calculator_has_uia` — Calculator only reports `['vision']`, not `'uia'`
  - Filed #785 (elevated to P0 after further investigation)
- **E2E**: 9 passed, 3 xfailed, 1 xpassed

## Phase 1 — Dev Verification
- No `status:done` issues pending verification

## Phase 2 — E2E Testing
- **Notepad**: PASS — see→click→type→capture→quit full flow working
  - Text typed correctly, verified via screenshot
  - JSON output valid (`-j | python -m json.tool`)
  - App quit with unsaved changes: works
- **Calculator**: FAIL — `app launch calculator` reports success but Calculator invisible to `list apps` and `see --app calculator`. PID mismatch (launcher PID vs actual UWP process PID). Filed #785 with P0/silent-failure.

## Phase 3 — Exploratory Testing
All pass:
- Empty string type: proper error, exit 1
- Nonexistent app/element: proper error, exit 1
- Special characters (`<>&"'\`~!@#$%^&*()`): typed correctly, verified in screenshot
- JSON error output: valid JSON, exit code 1
- Negative depth (`--depth -1`): boundary error, exit 1
- JSON mode click error: valid JSON, exit 1

## Phase 4 — Power User Simulation
- `--app` filter precision: PASS — no cross-contamination between notepad/explorer/terminal
- Title-based window targeting: PASS — `--app "Hello - Notepad"` works
- Rapid `see` calls: each takes ~15 seconds (includes AI vision processing) — usable but slow for power users
- `app launch explorer`: opens Program Manager (desktop shell), not File Explorer window — known behavior

## Phase 5 — Regression Test Results
| TC | Title | Result |
|----|-------|--------|
| TC-0003 | Chinese app name matching | PASS (23 consecutive) |
| TC-0008 | Multi-window targeting | PASS |
| TC-0011 | App filter cross-process | PASS |
| TC-0015 | App quit silent failure | PASS (9 consecutive) |
| TC-0031 | Notepad menu click targeting | FAIL — click reports success but menu doesn't open |
| TC-0035 | Press standalone modifier | PASS — alt activates menu bar |
| TC-0036 | Quit Chinese app name | PASS |
| TC-0038 | UWP Notepad enumeration | PASS (10 consecutive) |
| TC-0039 | App ID filter see | PASS |
| TC-0040 | Unicode capture path | PASS (9 consecutive) |
| TC-0041 | JSON mode exit code | PASS (3 consecutive) |

**Regression: 10/11 passed, 0 retired this round**

## Phase 6 — Test Case Evolution
- **Created**: TC-0043 (regression/calculator-uwp-invisible.yaml) — P0: Calculator UWP invisible after launch (#785)
- **Total test cases**: 44 (26 active, 18 retired/blocked)

## Phase 7 — Issues Filed
- **#785** — ci/bug: Calculator UIA detection fails + UWP Calculator completely invisible (P0, silent-failure)
- **#786** — bug: click eN on UWP Notepad menu items still does not open menu (regression from #681/#672) (P1)

## Top 3 Risks
1. **UWP Calculator completely invisible** (#785) — `app launch calculator` silently fails. PID mismatch means naturo can't track UWP app launches. This likely affects other UWP apps too.
2. **Menu click regression** (#786) — Fixed twice before (#672, #681) but regressed. UWP Notepad menu items are unclickable via `click eN`, blocking full automation workflows.
3. **No status:done issues for 48+ hours** — QA verification pipeline is idle. Dev velocity may be paused or work is happening without proper status labels.
