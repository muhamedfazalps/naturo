id: TC-0062
title: "dialog detect / taskbar list / tray list -j silently return success:true [] in NO_DESKTOP_SESSION"
status: active
category: exploratory
priority: P1
source_issue: 875
created: 2026-05-28
last_run: 2026-05-28
last_result: fail
consecutive_passes: 0
notes: |
  R134: FAIL — taskbar list -j={success:true,items:[],count:0} exit 0; tray list -j={success:true,icons:[],count:0} exit 0; dialog detect -j={success:true,dialogs:[],count:0} exit 0. All silently succeed in NO_DESKTOP_SESSION. #875 unchanged on HEAD 1323777.
  R137: FAIL — same shape via MCP wrapper too: `dialog_detect`/`taskbar_list`/`tray_list` MCP tools return success:true with empty arrays and isError:false in NO_DESKTOP_SESSION. CLI + MCP both bypass guard. HEAD `70e6591`.
  R138: FAIL — re-confirmed on HEAD `ceecfd5`. All three CLI commands still silently succeed. R138 also discovered the **wait family** (`wait --gone`) is in the same bypass class — filed as #893 (cross-listed on #885 epic). The silent-success surface is now 4 CLI families (app windows / dialog detect / taskbar list / tray list / wait --gone) + 2 MCP (capture_screen / list_windows) — all closeable by the centralized middleware in #885.
  R139: FAIL — re-confirmed on HEAD `16adca7` during Chinese User persona. All three still return success:true [] exit 0. No regression, no progress; #885 still open as the consolidating fix.

preconditions:
  - "naturo is installed and in PATH"
  - "Session has no interactive desktop access (SSH / RDP-blocked / service session)"

steps:
  - description: "taskbar list returns NO_DESKTOP_SESSION error envelope in no-desktop session"
    command: "naturo -j taskbar list"
    expect: '{"success": false, "error": {"code": "NO_DESKTOP_SESSION", ...}} with exit 1 — NOT {"success": true, "items": [], "count": 0}'
  - description: "tray list returns NO_DESKTOP_SESSION error envelope in no-desktop session"
    command: "naturo -j tray list"
    expect: '{"success": false, "error": {"code": "NO_DESKTOP_SESSION", ...}} with exit 1 — NOT {"success": true, "icons": [], "count": 0}'
  - description: "dialog detect returns NO_DESKTOP_SESSION error envelope in no-desktop session"
    command: "naturo -j dialog detect"
    expect: '{"success": false, "error": {"code": "NO_DESKTOP_SESSION", ...}} with exit 1 — NOT {"success": true, "dialogs": [], "count": 0}'

pass_criteria: "All three commands return the NO_DESKTOP_SESSION envelope (matching list apps), and exit code is 1"
fail_action: "Update or reopen #875"