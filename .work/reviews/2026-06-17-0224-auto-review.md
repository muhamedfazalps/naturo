# Orc autonomous review — 2026-06-17 02:24 (local) / ~01:24Z

**Headline: v0.3.2 ship-gate is one QA verification away.** The last remaining ship-gate
bug, **#843** (capture omits same-PID popup menus), had its **Dev fix merged** this cycle
(PR **#948**, `73d7d32` — Z-order-aware compositing of `capture --app/--pid` windows). All
machinery for v0.3.2 is now in place; only QA's real-desktop confirmation of #843 remains.

## State of the board
- **CI:** `develop` **GREEN** — Build & Test + CodeQL both `success` on HEAD `73d7d32`.
- **Open PRs:** **none.**
- **status:in-progress:** empty (after this cycle's #843 flip).
- **status:done (awaiting QA):** **#843** only.
- **needs:ace live queue:** #935, #915, #914, #860, #842 (unchanged).

## Actions taken this cycle
1. **Post-merge handoff for PR #948 → #843.** PR #948 (`fix/issue-843-zorder-composite`)
   merged to develop at 17:32Z (`73d7d32`); branch already deleted (Rule 14 satisfied — no
   stale ref). Issue #843 was still `status:in-progress`, so flipped it
   `status:in-progress` → `status:done` and posted a QA handoff comment describing exactly
   what to verify (open a #32768 popup via input, then `capture --app/--pid`, confirm the
   menu survives compositing). This is the handoff that prevents premature closure.
2. **Verified prior handoffs are clean:** #862 (PR #947, macos split) already
   **verified+closed** by QA. No other merged-but-unflipped issues.
3. **Reconciled the needs:ace queue:** #863 (input-verification "impossible" premise,
   disproven 01:15Z) no longer carries the `needs:ace` label — it is now a QA-owned
   `from:qa` issue for QA to close. NEEDS-ACE.md still lists it under "recommended for
   closure" (correct — QA owns the close). #915 (QA-down) remains `needs:ace`, still
   recommended for closure pending Ace's durability confirm.

## Ship-gate v0.3.2 — status
- Req (1) epic #885 (silent-failure cluster): **MET** (verified+closed 2026-06-16).
- Req (2) verify the 5 `status:done` bugs on a real desktop:
  - **#786 / #788 / #807 / #840** — **VERIFIED + CLOSED** 2026-06-17 01:15Z (input proven to
    work unattended via QA's probe-first gate, `19a72cd`).
  - **#843** — **fix merged (PR #948), now `status:done` awaiting QA.** Last blocker.
- **Net: when QA verifies #843, req (2) is fully met → cutting v0.3.2 (#914) becomes Ace's call.**

## Drive-the-product (Step 3)
- **Standing #1 priority — recognition supremacy:** two QA-verified framework proofs in place
  (#931 coverage benchmark, #933 Electron CDP). Next recognition move remains **#932** (Java
  Swing/SWT JAB owned-fixture proof — implemented but not benchmark-measured; env-blocked, no
  Java app on the desktop) and **#937** (QA validate the benchmark on mature external apps).
  Both already filed and sharp; no new issue needed.
- Backlog is well-stocked and sharp for Dev: ~30 `from:qa` contract/envelope bugs (#867–#901)
  + recognition #920/#932/#934/#937. No gap warranting a new speculative issue this cycle.

## Step 3.5 — weekly competitiveness
**Not due** — tracker baseline row is 2026-06-16; <7 days elapsed. Skipped per cadence.

## Top-3 next moves
1. **QA verifies #843** → flip `verified` + close → req (2) complete → surface ship-gate
   go/no-go to Ace (#914).
2. Pull **#932** (Java JAB proof) forward when a Java app is available; otherwise Dev picks
   from the `from:qa` contract backlog.
3. Ace check-in: close **#915** (QA durability proven) and decide **#935** (per-worktree
   lock) + **#914** (ship-gate timing).

**[Orc-Mycelium]**
